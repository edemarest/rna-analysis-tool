def calculate_stability(inputs):
    sequence = inputs.get("sequence", "")
    
    if not sequence:
        return {"error": "No sequence provided"}

    # Placeholder logic
    return {"stability_score": -15.6}
